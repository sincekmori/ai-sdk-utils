---
"ai-sdk-catalog": minor
---

feat(catalog): fill a model's missing `cost` from an embedded models.dev price snapshot resolved via the model's vendor (direct vendor or gateway backend); an explicit `cost` in the config always wins
